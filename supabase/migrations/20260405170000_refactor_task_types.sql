-- ============================================================
-- Refactor taskType ให้ clean และ consistent
-- ไม่มี emoji, ภาษาไทยหมด, ไม่ซ้ำซ้อน
-- "จัดยา" ไม่แตะ
-- ============================================================

-- === A_Tasks ===

-- ดูแลร่างกาย: รวม Foot Care, Mouth Care, Facial Care เข้ากับ ความสะอาด
UPDATE "A_Tasks" SET "taskType" = 'ดูแลร่างกาย' WHERE "taskType" IN (
  'ความสะอาด',
  'Foot Care (การดูแลเท้า)',
  'Mouth Care (ความสะอาดช่องปาก)',
  'Facial Care (ดูแลหนวดเคราหน้าตา)'
);

-- อาหาร: รวม Feed, อาหารเสริม, นม
UPDATE "A_Tasks" SET "taskType" = 'อาหาร' WHERE "taskType" IN (
  'Feed (สูตรอาหารปั่น)',
  'Feed',
  'อาหารเสริม',
  'นม'
);
-- 'อาหาร' เดิมไม่ต้อง update (ชื่อเดิมถูกแล้ว)

-- น้ำ: ชื่อเดิมถูกแล้ว ไม่ต้อง update

-- ยานอกมื้อหลัก: รวม ให้ยา + ยา
UPDATE "A_Tasks" SET "taskType" = 'ยานอกมื้อหลัก' WHERE "taskType" IN (
  'ให้ยา',
  'ยา'
);

-- กายภาพ: รวม กายภาพเบื้องต้น + กายภาพบำบัด
UPDATE "A_Tasks" SET "taskType" = 'กายภาพ' WHERE "taskType" IN (
  'กายภาพเบื้องต้น',
  'กายภาพบำบัด'
);

-- กิจกรรม: รวม นั่งเล่น + Mood
UPDATE "A_Tasks" SET "taskType" = 'กิจกรรม' WHERE "taskType" IN (
  'นั่งเล่น',
  'Mood (อารมณ์)'
);
-- 'กิจกรรม' เดิมไม่ต้อง update

-- หัตถการ: รวม ทำแผล, แผล, Bed Sore, ดูดเสมหะ
UPDATE "A_Tasks" SET "taskType" = 'หัตถการ' WHERE "taskType" IN (
  'หัตถการพยาบาล',
  'ทำแผล',
  'แผล',
  'Bed Sore (ตรวจสอบดูแลแผลกดทับ)',
  'ดูดเสมหะ (Suction) '
);

-- ขับถ่าย: รวม Bowel Movement, Urine
UPDATE "A_Tasks" SET "taskType" = 'ขับถ่าย' WHERE "taskType" IN (
  'Bowel Movement (การขับถ่าย)',
  'Urine (ปัสสาวะ)',
  'การขับถ่าย'
);

-- Insulin: แยกออกจาก DTX (ถ้ามี)
-- DTX เดิมชื่อถูกแล้ว ไม่ต้อง update

-- อื่นๆ: รวม ตามงาน, อัพเดต, คู่มือ, เอกสาร, คำสั่งแพทย์
UPDATE "A_Tasks" SET "taskType" = 'อื่นๆ' WHERE "taskType" IN (
  'ตามงาน',
  'อัพเดต',
  'คู่มือ',
  'เอกสาร',
  'คำสั่งแพทย์'
);
-- 'อื่นๆ' เดิมไม่ต้อง update

-- ที่เหลือไม่ต้อง update:
-- 'พลิกตะแคงตัว' ✓
-- 'ความปลอดภัย' ✓
-- 'งานในโซน' ✓
-- 'ชั่งน้ำหนัก' ✓
-- 'วัดส่วนสูง' ✓  (ถ้ามี)
-- 'DTX' ✓
-- 'สัญญาณชีพ v/s' ✓
-- 'จัดยา' ✓ (ไม่แตะ)

-- === C_Tasks (ส่วนใหญ่คือ จัดยา ไม่ต้อง update) ===

-- รวม taskType ที่ซ้ำใน C_Tasks ด้วย
UPDATE "C_Tasks" SET "taskType" = 'ขับถ่าย' WHERE "taskType" = 'การขับถ่าย';
UPDATE "C_Tasks" SET "taskType" = 'อื่นๆ' WHERE "taskType" IN ('ตามงาน', 'อัพเดต', 'คำสั่งแพทย์');
-- 'จัดยา', 'สัญญาณชีพ v/s', 'DTX', 'งานในโซน' ไม่ต้อง update

-- ============================================================
-- สรุป taskType หลัง refactor (ทั้ง A_Tasks + C_Tasks):
--
-- อาหาร          — ป้อนข้าว, อาหารปั่น, นม, อาหารเสริม
-- น้ำ            — ให้น้ำ, เช็คปริมาณน้ำ
-- ดูแลร่างกาย     — อาบน้ำ, แปรงฟัน, ตัดเล็บ, สระผม, ดูแลเท้า/ปาก
-- พลิกตะแคงตัว    — พลิกตัว (ทำบ่อย แยกออกมา)
-- กายภาพ         — กายภาพเบื้องต้น + กายภาพบำบัด
-- กิจกรรม        — นั่งเล่น, ดูทีวี, เดินเล่น, กระตุ้นสมอง
-- ยานอกมื้อหลัก   — ให้ยาที่ไม่ใช่งาน "จัดยา"
-- หัตถการ        — ทำแผล, Bed Sore, ดูดเสมหะ, หัตถการพยาบาล
-- ชั่งน้ำหนัก     — measurement: weight (kg)
-- วัดส่วนสูง      — measurement: height (cm) [ถ้ามี]
-- DTX            — measurement: blood glucose (mg/dL)
-- Insulin        — measurement: insulin (units) [ถ้ามี]
-- สัญญาณชีพ v/s  — vital sign photo system
-- ขับถ่าย        — ดูแลการขับถ่าย, ปัสสาวะ
-- ความปลอดภัย    — ตรวจราวกั้นเตียง, fall risk
-- งานในโซน       — ล้างเหยือก, เช็ดโต๊ะ
-- อื่นๆ          — ตามงาน, อัพเดต, คู่มือ, เอกสาร, คำสั่งแพทย์
-- จัดยา          — ❌ ไม่แตะ (ระบบ medicine แยก)
-- ============================================================
